import styled from "styled-components";

export const Input = styled.input`

    height: 48px;
    padding: 0 16px;
    background-color: var(--grey-2);
    border-radius: 4px;

    font-size: 16px;
    font-weight: 400;
    color: var(--white);
`