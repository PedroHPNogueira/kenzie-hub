import styled from "styled-components";

export const Select = styled.select`

    height: 48px;
    padding: 0 16px;
    background-color: var(--grey-2);
    border-radius: 4px;

    font-size: 16px;
    font-weight: 400;
    color: var(--white);

    margin-bottom: 5px;
`